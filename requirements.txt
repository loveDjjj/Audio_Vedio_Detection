# Recommended Python: 3.8
# External tools still required outside pip:
# - 7-Zip (for dataset/download_av1m_meta.py)
# - ffmpeg (for extracting 16kHz mono audio from raw mp4 during AV-HuBERT training)
# - AV-HuBERT assets under resources/

# Core runtime
numpy<1.24
PyYAML
tqdm
requests
huggingface_hub

# Training
torch

# AV-HuBERT / preprocessing
opencv-python-headless==4.5.4.60
scikit-image==0.17.2
dlib==19.24.6
python-speech-features==0.6
scipy==1.10.0
sentencepiece==0.1.96
editdistance==0.6.0

# fairseq dependencies used by the vendored AV-HuBERT repo
cffi
cython
hydra-core<1.1
omegaconf<2.1
regex
sacrebleu>=1.4.12

# Install the vendored fairseq checked out in third_party/av_hubert
-e ./third_party/av_hubert/fairseq
